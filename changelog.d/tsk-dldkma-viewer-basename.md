### Fixed

- Image Viewer and Media Player now display only the basename (e.g. `photo.png`) in the window title/header when a routed URL encodes a nested path (e.g. `nested%2Fphoto.png`) instead of the full decoded path.
