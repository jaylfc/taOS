### Added
- Notifications app with Notifications and Archive tabs; Notification Archive content is now reachable inside the Notifications surface as an Archive/History tab.
- `notification-archive` app is hidden from the launcher (tier 3) and dock pins redirect to `notifications` with `section: "archive"`.
