### Added
- Unified chat exporter gains `dry_run_channel` (author coverage report) and `import_channel` (idempotent batch import to the taOSmd A2A bus `/a2a/import` endpoint with explicit author mapping enforcement).
